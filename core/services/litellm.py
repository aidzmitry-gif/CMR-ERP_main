"""AI-gateway (LiteLLM): роутинг локал↔API, учёт токенов. Подключается с AI-слоя.

В прототипе не используется — AI вынесен в Итерацию 1. Сервис существует в ядре,
чтобы AI-подмодули модулей позже обращались к общему шлюзу, а не к моделям
напрямую (см. §2.5 и §3.3 архитектуры).
"""
from __future__ import annotations


class LLMGateway:
    """Заглушка AI-шлюза. Включается на этапе AI-слоя."""

    def __init__(self) -> None:
        self.enabled = False
