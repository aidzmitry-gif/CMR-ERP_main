# Задание: доска Сделок — доделать внешний вид (финиш визуальной линии)

> Постановка: оператор через координатора, 2026-07-03. Полоса: **sales** (`1b5d8576`).
> Приоритет: СНАЧАЛА это, потом бэк-расчёт метрик скорборда (следующее задание, уже выбрано оператором).

## Контекст

Слайсы П1–П5 + П6-приход из `_tz_sales_plan_fact_ui.md` реализованы (коммиты `327cfd3`, `81ad870`,
`03415d6`, `c9582ef`, `f50e753` — все уже в локальной ветке после реконсиляции координатора).
Осталось довести карточку и вычистить мелочь. Реальные данные ноя-2024/дек-2025 в dev.db,
бэк на `127.0.0.1:8000` (IPv4 — не менять биндинг), фронт :3000.

## Объём (в порядке выполнения)

1. **П6-остаток — бейджи карточки сделки** (по `_tz_sales_plan_fact_ui.md` §П6):
   - **act-badge «Сегодня/Завтра»** — разблокирован: datetime «Следующий шаг» уже есть (П4, `0089`).
     Красить по `next_step_at`: сегодня / завтра / просрочен.
   - **cur-base «≈ N Br»** — при валюте сделки ≠ базовой ЮЛ (CurrencyProvider уже в AppShell).
   - **«★ Пост.»** — бейдж сделок воронки `repeat_clients`.
2. **React key warning** в `DealsWorkspace` (консоль: «Each child in a list should have a unique
   "key" prop… child from DealsPage») — найти список без key, починить. Находка ui-crawl координатора.
3. **Финальная визуальная сверка** доски с эталоном `sales-board-mockup.html` на реальных данных:
   светлая + тёмная (класс `dark` на `<html>`), узкий вьюпорт 390px. Расхождения с макетом —
   списком оператору (не чинить молча, если спорно: правило «не выкидывать элементы»).

## Ограничения (координатор)

- **Миграции НЕ заводить**: `0088` (tenders) и `0089` (next_step_at) уже пронумерованы и в ветке;
  голова = `0089`. Новый номер — только через координатора.
- Хотспот `frontend/src/lib/api.ts` сейчас свободен; захват — строкой координатору.
- Скорборд (структуру PlanFactCell/PRIMARY_CELLS) не перестраивать — только карточка/полировка.
- Коммиты локально в `sales-2.0-redesign`, БЕЗ push (18 коммитов ждут отмашки оператора пакетом).
- Скриншоты — через Playwright MCP (preview_* зависает на вечном SSE /crm/*).

## DoD

- [ ] 3 бейджа живут на карточке и видны на реальных сделках (скрин light+dark).
- [ ] Консоль доски чистая (key warning ушёл).
- [ ] tsc + vitest зелёные; тесты на бейджи (минимум: act-badge границы сегодня/завтра/просрочен).
- [ ] Список расхождений с эталоном сдан оператору (или «расхождений нет»).
- [ ] Строка `КООРД: DONE sales — …` с номером коммита.
