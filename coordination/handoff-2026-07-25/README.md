# Пакет активов Cursor-у — ночь 2026-07-25

Задание: [`../task-cursor-night-2026-07-25.md`](../task-cursor-night-2026-07-25.md) (пункт P3, §7).

| Файл здесь | Куда класть |
|---|---|
| `planning.py` | `modules/leads/planning.py` (submodule CRM-LID1.1, коммит туда) |
| `test_leads_planning.py` | `tests/unit/test_leads_planning.py` (суперпроект) |

**Состояние на момент передачи (прогнано координатором 2026-07-25):** `pytest` — 15 passed,
`ruff check` — чисто.

**Зависимости:** `planning.py` импортирует только stdlib (`math`, `dataclasses`, `decimal`),
тест — только `modules.leads.planning`. С кодом лидов не пересекается, поэтому базой можно брать
любой чекаут от `165a5fc` и выше.

**Что даёт:** `reverse_funnel` (цель по выручке → сделки → готовые лиды → тёплые обращения →
сырые лиды, `ceil` на каждом отчётном уровне, деньги в `Decimal`) и `capacity_check` (сверка снизу
вверх: физическая ёмкость лидоруба против потребности). Битый коэффициент (0, >1, отрицательный
чек) — `ValueError`, а не `inf`: план на мусорных числах опаснее отсутствия плана.

**Чего здесь НЕТ намеренно:** таблиц `lead_plan_*`, статус-машины согласования, миграции. Ночью
делается stateless-слайс — см. P3 задания. Причина запрета на миграцию — §0.3 (untracked `0106`
вне git).
