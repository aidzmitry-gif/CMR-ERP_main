# Ретроспектива дня — 2026-06-20

> Авто-отчёт (read-only анализ). Источник дня — **mtime файлов + STATUS/FLEET + git status**
> (в авто-режиме 23:59 `git log/diff` требуют подтверждения → недоступны, см. [[auto-retro-no-git-log]]).
> Логи `.quality-log.jsonl` / `.tg-guard-denied.jsonl` за 20.06 — пусты (артефакты дня —
> `.docx`/`.html`, не код под ruff/tsc-гейтом).

## TL;DR
- День **спеков и макетов, не кода**. Две нити: (1) **ТЗ для 1С-специалиста по OData**
  (`ТЗ_специалисту_1С_OData.docx`+`.md`, 14:47–14:55) и (2) вечерняя волна **HTML-макетов
  Sales 2.0** (23:23–23:50): постоянные клиенты, чат-превью на доске, доска, сквозной статус машины.
- Коммит дня — `be5e884 feat(sales-ui): доска — авто-стадии/задачи/звонок/приоритет, видимость по ролям`
  (это `sales-board-mockup.html`).
- **Качество-гейты молчали** — за весь день ни одного ruff/tsc-чекпоинта и ни одной guard-деny:
  работа шла в форматах вне гейта. Проверка деливераблов дня — глазами по макетам, не авто.
- **Главный риск дня — копится невлитый WIP.** Воркеры `deals-board-fe` и `chats-panel-fe`
  стоят **COMPLETE-но-не-влитыми 8 дней**, а вечером руками рисовались HTML-макеты доски и
  чатов — те же области. Растёт расхождение «макет ↔ уже собранный Next.js».
- **Наблюдаемость и CI не сдвинулись с 15.06:** `FLEET.md` — снимок 05:00Z, **CI(main): failure**
  (красный ≥5 дней), флот из ~15 воркеров 5–8-дневной давности. Это ровно те 3 пункта,
  что я выписал «на завтра» 15.06 — **не закрыты**.

## Что сделано (по mtime)
- **1С / OData — ТЗ для админа** (14:47–14:55): `ТЗ_специалисту_1С_OData.docx` + `.md`.
  Продолжение [[onec-odata-setup]] (1С КА 2.4 на `192.168.89.14`, IIS стоит, пользователь
  настраивает по RDP). Цель — питать коннекторы и досье 360° [[client-360-bitrix-1c]].
- **Sales 2.0 — 4 HTML-макета** (вечер, 23:23–23:50), нить [[sales-2.0-design]]:
  - `sales-regular-clients.html` — постоянные/перезаказ (SALES-48).
  - `sales-board-chat-preview.html` — чаты на доске [[crm-chats-messenger]].
  - `sales-board-mockup.html` — доска (= коммит `be5e884`): авто-стадии/задачи/звонок/приоритет,
    видимость по ролям.
  - `sales-shipment-status.html` — сквозной статус машины.
- **Параллельная нить вне этого репо — Olivia** (guard-лог фиксирует ожидание `pip install diarize`
  в `d:/6 Проекты/Olivia`): диаризация для privacy-щита. Совпадает с [[parallel-sessions-sync]],
  [[olivia-local-run-2026-06-18]].

## Мои паттерны (пользователь)
- **HTML-first для новой UI-логики.** Прежде чем трогать Next.js, концепция обкатывается в
  self-contained HTML-макете (сегодня — доска/чаты/статус машины/постоянные). Тот же почерк,
  что и в Sales 2.0 / закупках / маркетинге.
- **Спек как деливерабл.** ТЗ для внешнего 1С-специалиста вынесено в `.docx` (для человека вне
  Claude/репо) — повторяет вчерашний `Инструкция_1С_OData_для_админа.docx`.
- **Поздние широкие сессии.** Основной объём — после 23:00 четырьмя файлами подряд; днём —
  один спек-блок. Совпадает с «много параллельных нитей» [[parallel-sessions-sync]].

## Наблюдаемость / флот
- `FLEET.md` = снимок **2026-06-20 05:00Z**, **CI(main): failure**. Таблица почти вся пустая:
  - **COMPLETE, но НЕ влиты:** `conn-tests-bitrix/google/onec` (8д), `sprav-merge/import/categories`
    (гейт ЗЕЛЁНЫЙ). `chats-panel-fe`, `deals-board-fe` — в [[orchestrator-integrate-gotcha]]
    числятся COMPLETE, в FLEET без state, **8 дней в worktree**.
  - `rop-stages` — застрял в `SPAWNED` (6 дней).
- За день **новых воркеров не запускалось**, интеграций не было — работа шла соло, руками.

## Трение и риски
- **Дубль-риск «макет ↔ воркер».** Вечерние `sales-board-mockup.html` и `sales-board-chat-preview.html`
  пересекаются с `deals-board-fe` и `chats-panel-fe`, которые **8 дней готовы, но не влиты**.
  Либо макеты — это переосмысление (тогда воркерский код устаревает, его правда не в main),
  либо дубль работы. Нерешённый дубль чатов в UI уже отмечен в [[crm-chats-messenger]].
- **CI(main) красный ≥5 дней.** Был `failure` 15.06, остаётся `failure` 20.06. Любой merge
  идёт на красное дерево — доверие к «зелёному» потеряно.
- **Гейты не покрывают деливераблы дня.** `.docx`/`.html` не проходят ruff/tsc → у спеков и
  макетов нет автопроверки (битые ссылки/наложения/переносы). Для HTML это закрывается
  скиллом `ui-crawl` — сегодня не прогонялся.
- **Невозможно собрать точный журнал дня** в авто-режиме (git log заблокирован). Отчёт — по
  косвенным следам; уникальность/авторство коммитов не верифицированы.

## Победы дня
- **1С-интеграция разблокирована со стороны процесса**: внешнему специалисту отдано внятное ТЗ
  по OData — снимает зависимость «жду доступ к 1С» для коннекторов/досье 360°.
- **Sales 2.0 продвинулся вширь**: 4 новых экрана-сценария (постоянные, чаты на доске,
  доска c авто-стадиями/ролевой видимостью, сквозной статус машины) — материал для следующего
  порта в Next.js.

## 3 улучшения на завтра
1. **Закрыть 8-дневный «integrate или stop» ДО новых макетов доски/чатов.** Перед следующей
   итерацией Sales-доски — решить судьбу `deals-board-fe` и `chats-panel-fe` (влить или
   осознанно выкинуть), иначе HTML-макеты и Next.js-код расходятся вслепую. (Тот же пункт висит
   с 12.06, см. [[orchestrator-integrate-gotcha]].)
2. **Починить красный CI(main).** Висит `failure` ≥5 дней (15.06 → 20.06). Это **повтор пункта
   из ретро 15.06**, не сделано. Диагностировать и закрыть до любой новой волны/merge.
3. **Прогонять `ui-crawl` по вечерним HTML-макетам.** Деливерабл дня — 4 HTML без автопроверки.
   Быстрый click-through + проверка вёрстки (наложения/переносы/читаемость, обе темы) даёт
   макетам ту же дисциплину, что ruff/tsc даёт коду.

> ⚠️ Сквозной сигнал двух ретро подряд (15.06 и 20.06): **наблюдаемость (FLEET 05:00Z),
> красный CI, и невлитый флот** — не закрываются. Это уже не «на завтра», а накопленный
> процессный долг; стоит выделить отдельную сессию-уборку, а не двигать дальше по фичам.
