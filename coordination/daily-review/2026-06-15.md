# Ретроспектива дня — 2026-06-15

> Авто-отчёт (read-only анализ). Источники: `commits-today.txt` (69 записей),
> `.quality-log.jsonl`, `.tg-guard-denied.jsonl`, `.workers-state.json`, `FLEET.md`,
> `sprav-*-status.md`, `orchestrator-answers/`, `memory/pitfalls.md`, транскрипты сессий.
> _Первый отчёт в этой папке — раньше ретроспектив не велось._

## TL;DR
- Главный итог дня — **вкладка «Справочники» (Reference Data / MDM)**: ядро Ф0–Ф5 +
  все **7 фронт-экранов** портированы одной оркестрованной волной воркеров, **7/7 → COMPLETE**.
- Параллельно — большой пласт **Sales 2.0** (окно звонка, документооборот, каталог-подбор,
  карточка номенклатуры, прогноз маржи на доске) и прототип блока **«Контролёр ОП»**.
- **Качество держалось:** 14 хук-чекпоинтов за день, **все ruff+tsc зелёные** (0 блоков гейта);
  `/code-review` ловил реальные баги ДО merge.
- **Трение:** баг обрезки первого промпта воркера (чинили на ходу), один воркер умер посреди
  `/code-review` (пере-нудж), и десятки ложных деnaи guard-а на легитимном аудите .env/гарда.
- **Наблюдаемость отстала:** `FLEET.md` (снимок 05:00Z) и `.workers-state.json` НЕ отражают
  волну sprav-*; CI(main) был красный утром.

## Мои паттерны (пользователь)
> Транскрипты сканировал сабагент; **числовые оценки приблизительны** (вероятно, посчитал и
> tool-шум). Качественные темы подтверждаются коммитами/логами — на них и опираюсь.

- **Детальный спек на новое → терсные правки на итерации.** Новые концепции диктуются длинно
  (голосовой ассистент / chief-of-staff), а доводка идёт короткими сужениями скоупа:
  «только то что касается сделок», «план на день должен быть в сделках», «все мои звонки писать
  нужно». Это же видно в коммитах-правках «по фидбеку» (`fix(sales-2.0/call): правки по фидбеку`).
- **Тяжёлое делегирование.** 7 экранов «Справочники» отданы флоту воркеров (1 экран = 1 воркер);
  ревью безопасности и визуальная сверка макетов — тоже делегированы. Руками — постановка
  задач, сужение требований, приёмка по скриншотам (в корне репо десятки `card-*.png`,
  `call-*.png`, `board.png`).
- **Много параллельных нитей за день**: справочники-оркестрация, Sales 2.0, контролёр,
  безопасность, голосовой ассистент. Совпадает с заметкой [[parallel-sessions-sync]].
- **Сквозные требования-лейтмотивы**: «любое обращение/звонок фиксируется и маршрутизируется»
  [[call-message-routing-rule]] и «видимость маржи/landed cost» [[sales-margin-analysis]] —
  оба отражены в сегодняшних коммитах (call-routing, «прогноз маржи» на доске).

## Агенты/воркеры
- **Волна «Справочники» (7 воркеров) — 7/7 COMPLETE.** Подтверждение: `STATE: COMPLETE` в
  `sprav-{catalog,rates,merge,card,ai,categories,import}-status.md`. Экраны 1–7.
- **Гейты соблюдены:** каждый экран с acceptance-gate (напр. `sprav-import` 6/6 GREEN,
  `sprav-categories` 6/6), `tsc --noEmit` чисто, vitest зелёный (catalog 9/9, rates 10/10,
  ai 12/12, categories 14/14, card 8/8, merge 5/5). **Ложных COMPLETE гейт не поймал.**
- **Авто-харвест граблей:** воркеры зарепортили pitfalls → собраны в `memory/pitfalls.md`
  (коммиты `chore(memory): harvest 1 pitfall from sprav-ai|sprav-rates`):
  - sprav-rates: `title` на lucide-иконке → TS2322 (оборачивать `<span title>`); `pitfalls.md:85`.
  - sprav-ai: хук `/code-review` напомнил уже ПОСЛЕ коммита → гонять ревью на staged ДО commit; `pitfalls.md:81`.
- **Прежний флот простаивает невлитым:** `chats-panel-fe`, `deals-board-fe` — COMPLETE, в main
  не влиты (worktrees `crm-worker-*`), решение «integrate или stop» висит со 2026-06-12
  (`ACTIVE-SESSIONS.md:49-51`). В `.workers-state.json` это старые воркеры (11–14 июня).

## Взаимодействия и хуки
- **Quality-хук (ruff+tsc):** сегодня 14 чекпоинтов (`.quality-log.jsonl:14–27`), **все
  `ruff_ok:true, tsc_ok:true`** — ни одного блока. Самый крупный заход — 9 файлов (7 py+2 fe).
- **`/code-review` приносил пользу до merge:** 5 фиксов в `sprav-catalog` (race-condition в
  `selectRef`, мёртвый импорт, `is_active` badge, key, toLowerCase — коммит `25e338b`);
  парсинг audit-даты в карточке (`57bd61d`); ревью вскрыло, что merge/unmerge не писали
  аудит-события → закрыто (`6324f7c`, тест 8/8).
- **Оркестратор↔воркеры:** 5 answer-файлов за день (`orchestrator-answers/20260615T*`):
  kickoff sprav-catalog/rates/merge/ai (15:04–15:06Z) и **resume sprav-import (19:36Z)** —
  процесс воркера оборвался посреди `/code-review`, оркестратор дослал «доведи до конца,
  не спрашивай вживую».
- **Guard (deny-хук):** сегодня 8 деnaи (`.tg-guard-denied.jsonl:36–43`) — все из
  сессии аудита безопасности: Read `.env` ×3, `.claude.json`, проверка трекинга `.env`,
  `tailscale ssh` на прод. Совпадает с [[security-posture-strategy]].

## Трение и потери
- **Баг обрезки первого промпта воркера.** Воркеру приходил только заголовок
  `=== WORKER ENGINEERING STANDARDS ===`, тело терялось → «прислали пустоту». Чинили на ходу:
  `fix(orchestrator): подавать промпт через stdin` (`bad0ed8`) + защитная грабля «читай задание
  с диска» (`54867ec`, `pitfalls.md:24-30`). Потеря: как минимум один зависший старт.
- **Воркер умер посреди ревью** (sprav-import) → ручной/авто пере-нудж в 19:36Z. Лишний раунд.
- **Ложные срабатывания guard-а на своей же мета-работе.** Помимо 8 сегодняшних, накануне
  (14.06, `.tg-guard-denied.jsonl:1–35`) сессия харденинга гарда ~30 раз ловила собственный
  guard, пытаясь закоммитить/отредактировать `claude_guard_hook.py`; пофикшено word-boundaries,
  но Read `.env` для легитимного аудита всё ещё жёстко блокируется (3 ретрая впустую сегодня).
- **Наблюдаемость отстаёт от факта.** `FLEET.md` — снимок 05:00Z, в нём нет sprav-волны
  (она шла ~15:00–19:00) и стоит **`CI(main): failure`**. `.workers-state.json` тоже без
  sprav-*. Картину дня по дашборду в реальном времени было не видно.
- **Дубли в журнале коммитов:** часть из 69 записей — cherry-pick'и integrate-потока
  (напр. `fc1390d` ≡ `d743b35`, `db62ac0` ≡ `8af5b54`). Реальных уникальных коммитов меньше.

## Победы дня
- **Вкладка «Справочники» закрыта end-to-end за день**: backend Ф0–Ф5 (SCD2-сервис, generic-CRUD,
  MDM golden record, `reference.query` для AI, идемпотентный 1С-адаптер, иерархия категорий,
  миграции 0037/0039/0040) + **7/7 фронт-экранов COMPLETE** + аудит merge/unmerge.
- **Чистое качество**: 14/14 чекпоинтов зелёные; ревью отлавливало баги до merge, не после.
- **Самолечение процесса**: баг оркестратора найден и устранён в тот же день, грабли
  авто-вшиты в промпт будущих воркеров — следующая волна на них уже не наступит.
- **Sales 2.0 продвинулся широко**: правило записи/маршрутизации звонка, документооборот
  (реестр+архив+согласование), каталог-подбор с ИИ, «прогноз маржи» на доске под годовую цель.

## 3 улучшения на завтра
1. **Ревью ДО коммита, а не после.** Хук-напоминание `/code-review→/simplify` срабатывает на
   `Bash(git commit)` → приходит постфактум (грабля sprav-ai, `pitfalls.md:81`). Сделать
   шаг/обёртку, гоняющую `tsc --noEmit` + vitest + `/code-review` на **staged-diff до** commit
   (PreToolUse-проверка staged, либо явный пункт в worker-standards «review staged → commit»).
2. **Снять ложный деnaй guard-а для своей мета-работы.** Аудит безопасности и обслуживание
   самого гарда сегодня/вчера тратили десятки ходов на ретраи. Ввести явный «maintenance/audit»
   режим (env-флаг или confirm-обход) для **read-only** доступа оператора к `.env`/`.claude.json`
   и правке `claude_guard_hook.py`, оставив deny по умолчанию для воркеров.
3. **Синхронизировать наблюдаемость с волной.** Обновлять `FLEET.md`/`.workers-state.json`
   на старте и финише волны (или из `integrate`), а не раз в сутки — сегодня дашборд (05:00Z)
   на 14 ч отставал от реальности. Завести **красный CI(main) в дневной/флот-вид** и чинить
   до запуска новой волны (утром CI был failure).

### Предложение в `pitfalls.md` (НЕ применено — на ваше решение)
```
- **СИМПТОМ: воркер «исчез» посреди /code-review, STATE так и не COMPLETE.** Причина: процесс
  воркера завершился до финала (наблюдалось у sprav-import 2026-06-15). → ЛЕЧЕНИЕ: оркестратору
  детектить «процесс закончился без STATE: COMPLETE» и авто-дослать resume-ответ
  (как orchestrator-answers/20260615T193653Z_sprav-import.md), не дожидаясь ручного пинга.
```
