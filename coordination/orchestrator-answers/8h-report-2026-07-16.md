# Оркестратор 8h — итоговый отчёт (2026-07-16/17)

## Вердикт

План выполнен **частично по DoD**, прод жив (`dev`/`dev`), смоук зелёный. Полный OIDC-флип **не** делали (жёсткий стоп соблюдён).

| Полоса | Статус | Итог |
|--------|--------|------|
| **A** Keycloak URL | **SUCCESS** | `https://auth.belakb.by/realms/aios` — DNS+LE+openid **200** (2026-07-17) |
| **B** Frontend OIDC | **SUCCESS** | ADR + non-breaking Bearer proxy skeleton |
| **C** 1С live | **SUCCESS*** | OData live, sync 1000 stock / 1202 SKU; *патч skip-no-UNP на сервере через `docker cp` (нужен commit+rebuild) |
| **D** Secure-env | **SUCCESS** | Чеклист без флипа prod |
| **E** Смоук | **SUCCESS** | health/board/leads 200; alembic `0105`; HEAD `0c585d1` |

\* Sync `/integrations/1c/sync` → `{"ok":true,"counterparties":500,"stock":1000}`. Дополнительно ETL `extract-onec`/`load-onec` (`IMPORT_FROM=2025-12`) — 209 SKU/цен. Подбор `/sales/skus?for_picker=1` — реальные коды 1С (`00-…`, `КА-…`), не только демо.

## Действие оператора (блокер A)

Создать DNS: **`A` `auth.belakb.by` → `93.125.0.131`** (как `belakb.by`).  
После пропагации Caddy получит LE-сертификат; проверка:

```bash
curl -sS https://auth.belakb.by/realms/aios/.well-known/openid-configuration | jq .issuer
# expect: https://auth.belakb.by/realms/aios
```

## Артефакты

| Файл | Содержание |
|------|------------|
| `coordination/orchestrator-8h-2026-07-16.md` | План |
| `coordination/orchestrator-answers/fe-oidc-ready-2026-07-16.md` | Track B ADR |
| `coordination/orchestrator-answers/secure-env-flip-checklist-2026-07-16.md` | Track D |
| `frontend/src/lib/api-proxy-headers.ts` (+ test, route) | Bearer-ready proxy |
| `core/services/reference_import.py` | skip строк без УНП (локально + на сервере в контейнере) |

## Сервер (факт)

- App: `AIOS_ENVIRONMENT=dev`, `AIOS_AUTH_MODE=dev`
- `AIOS_ONEC_*` в `.env` + `docker-compose.override.yml` (проброс в app)
- OData с app: **200** (`http://100.75.171.85/ka_copy/...`)
- Создана отсутствовавшая таблица `survivorship_rule` (дыра после rename 0044→0087)
- cloudflared quick tunnel **не трогали**

## Не делать до готовности фронта

- ❌ `AIOS_ENVIRONMENT=prod` / `AIOS_AUTH_MODE=oidc`
- ❌ смена Postgres/`KC_DB_PASSWORD` без окна на recreate app

## Следующий шаг (коротко)

1. DNS `auth.belakb.by`  
2. Commit+push: `reference_import` skip-no-UNP + frontend proxy headers → deploy `--build`  
3. Plan A SSR Bearer / Keycloak login (см. FE ADR) → только потом oidc-флип  
