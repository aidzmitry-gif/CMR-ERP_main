<!-- Транзитный засев новой полосы. Не коммитить. Координатор 2026-06-27 — сверено адверсариально с кодом modules/wms (approved). -->
# Склад / WMS (SKL-5) — новый чат (полоса свободна, ты её берёшь)

Ты — полоса **Склад / WMS**, submodule **SKL-5** (`modules/wms`). Полоса была свободна — берёшь её. Ветка суперпроекта: `sales-2.0-redesign`. Запускайся из корня суперпроекта; правки субмодуля — через `git -C modules/wms`.

## Зона
- `modules/wms/**` (модель, роуты, события); фронт `frontend/src/app/erp/wms/**`, wms-компоненты.
- Схема БД `wms`, API-префикс `/wms`.

## 🔴 ГЛАВНЫЙ КОНТРАКТ — остатки (читай ПЕРВЫМ, это безопасность/деньги)
- Остатки/резервы — **ОПЕРАЦИОННЫЕ** данные, НЕ master, НЕ в справочниках.
- **1С = истина склада (фаза 1).** Шлюз остатков — `core.services.stock` (`StockGateway`: `reserve`/`release`/`stock_by_sku`/`batches_by_sku`); его реализация сейчас в `modules/integrations` (полоса СИНК-сессии). **НЕ трогай `core.services.stock` и `modules/integrations`.**
- WMS **ДУБЛИРУЕТ движения** (приход/расход/перемещение для операционного складского учёта), но **НЕ становится источником истины остатков**. Если строишь РЕАЛЬНЫЙ учёт, меняющий источник истины — это смена сейма → **сначала согласуй с СИНК-сессией и владельцем** (нельзя тихо разойтись с 1С-остатками). Канон — `coordination/mdm-data-class-seam.md`.
- Номенклатуру (`Sku`) бери из MDM (shared-kernel), **НЕ дублируй**.

## Входящие события (УЖЕ подписан — `module.py:19-21`, §2 DEPENDENCY-MAP)
- `procurement.received` + `production.completed` → `on_goods_received` (приход товара/готовой продукции).
- `sales.stock.reserved` → `on_stock_reserved` (резерв/списание под заказ).
- ⚠️ Обработчики **уже реализованы** в `modules/wms/events.py` и уже **дублируют факт движения в `StockMovement`** — это и есть «WMS дублирует движения». НЕ переписывай их с нуля и НЕ переводи в источник истины остатков.
- 🔴 **НЕ подписывайся на `logistics.import.arrived`/`customs_cleared`** — это двойной учёт. Физический приход импорта делает **procurement (QC) → `procurement.received` → wms**. События `logistics.import.*` — только информационные.

## Миграция
- **0056 зарезервирован за тобой** (схема `wms`), файл в суперпроекте `migrations/versions/`. Голова цепочки = **0055** (`0055_category_attributes.py`), `down_revision="0055"`. **Перед написанием файла пингуй координатора** вписать 0056 в счётчик (единый писатель) и подтвердить head.

## Координация (канон)
- Реестр (ACTIVE-SESSIONS/DEPENDENCY-MAP/STATUS/счётчик миграций) и `.claude/settings.json` — только координатор; пингуй. Писать тебе МОЖНО только в `coordination/.activity.local.md`/`PUSH-LOG.md` (авто).
- Не трогать: `core.services.stock`, `modules/integrations`, shared-kernel (`core/domain/models.py::Sku`, counterparty, ref_*), `core/services/*`, чужие submodules.
- Защита роутов — канон: `Depends(require_permission("<perm>"))` (AuthN P1-1 влит).
- Push: cherry-pick ТОЛЬКО своего коммита на чистую ветку от origin; правка субмодуля = коммит + bump указателя; никакого amend/reset/rebase на общей ветке. Push/commit — по явной просьбе.

## Следующий шаг (выбор с оператором)
- WMS сейчас — каркас: ровно **2 таблицы** (`StockMovement`, `WarehouseOp`). Базовое дублирование движений уже работает (обработчики выше). Первый шаг — **операционная модель/UI ПОВЕРХ** существующего дублирования: перемещение/инвентаризация/зоны хранения, фронт `erp/wms` (остатки/движения как **зеркало 1С**). НЕ переписывать дублирование и НЕ заводить остатки в обход 1С/StockGateway.
- Ждёт: контракт «импорт→склад» — НЕ от логистики (отозван), а через `procurement.received`.

В конце доложи координатору: что взял, нужен ли 0056, какие события подписал.
