Реализуй SALES-49: сворачиваемая панель «Чаты и дела» со счётчиком непрочитанных — **только
компонент `frontend/src/components/chats-panel.tsx`**.

Эталон поведения — `mockup_Сделки_2.0.html` в корне репо (правая панель: свёрнутая рейка с
аватарками и красными бейджами → раскрытие по наведению поверх доски). Полное ТЗ — раздел
SALES-49 в `ТЗ_Блок_Сделки_2.0.md`. Границы и acceptance — `coordination/chats-panel-fe-scope.md`.

Цель (Goal-Driven, TDD):
1. Напиши co-located тест `frontend/src/components/chats-panel.test.tsx`: в свёрнутом виде
   рендерятся аватарки контрагентов; при `unread > 0` показывается бейдж с числом. Сделай RED → green.
2. Переделай разметку/стили `chats-panel.tsx`:
   - по умолчанию — узкая рейка ~64–68px: только аватар + красный бейдж непрочитанных;
   - `:hover` по панели — раскрытие до ~300px **поверх** контента (overlay, не раздвигая доску),
     показываются название контрагента и последнее сообщение; уход курсора — сворачивание;
   - раскрытие — чистый CSS `:hover` (без JS-стейта ширины).
3. Счётчик непрочитанных — из необязательного поля диалога: **общий тип `ChatItem` НЕ меняй**
   (его расширит бэкенд/другой воркер). Читай как `item.unread ?? 0`; если 0/нет — бейдж не показываем.

Жёсткие границы: НЕ трогай `lib/api.ts`, `lib/types.ts`, `kanban/**`, `funnel/**`, `sidebar.tsx`,
`app/**`, `modules/**`, `*.py`. Если кажется, что без правки общего типа никак — STOP и
`STATE: NEEDS-ORCHESTRATOR-ANSWER` в status-файле (не трогай общий файл сам).

Верификация:
- если нет `frontend/node_modules` — один раз `npm install` в `frontend/`;
- `npx vitest run src/components/chats-panel.test.tsx` → 0;
- `npm run lint` — чисто по `chats-panel.tsx`;
- НЕ запускай `npm run build` (сломан на чужом `crypto/page.tsx`).

Финальный баннер `STATE: COMPLETE` — только когда vitest=0 и lint чист.
