Задание от оператора (через координатора): Справочники — внешний вид витрины на реальных данных.

Полное ТЗ: `coordination/reference-showcase-visual-scope.md`. Кратко:
1. Плитка «Номенклатура» в хабе `/erp/spravochniki` (сейчас её НЕТ — карточка SKU достижима только прямым URL, находка ui-crawl) → каталог 232 реальных SKU с поиском и фильтром по категории.
2. Кликабельный путь хаб → дерево категорий (37 реальных из 1С) → карточка `/erp/spravochniki/sku/[code]`; закрыть техдолг обхода дерева.
3. Сверка витрины на реальных данных: провенанс «из 1С» виден, длинные названия 1С не рвут вёрстку, голова+филиалы по УНП.
4. Light + dark + узкий вьюпорт. Honest-empty для пустого (SCD2-версии) — норма.

Ограничения: миграций НЕ надо (голова 0089, номера через координатора); `modules/integrations/**` не трогать; 1С read-only; коммиты локально в `sales-2.0-redesign` БЕЗ push; бэк на `127.0.0.1:8000`. Скрины — Playwright MCP.

DoD в ТЗ. По готовности: `КООРД: DONE reference — …` с коммитом.
