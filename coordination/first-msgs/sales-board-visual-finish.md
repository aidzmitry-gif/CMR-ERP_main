Задание от оператора (через координатора): доделать внешний вид доски Сделок — финиш визуальной линии, ПЕРЕД бэк-расчётом метрик скорборда.

Полное ТЗ: `coordination/sales-board-visual-finish-scope.md`. Кратко:
1. П6-остаток — три бейджа карточки: act-badge «Сегодня/Завтра» (по `next_step_at`, П4 уже в ветке), cur-base «≈ N Br» (CurrencyProvider), «★ Пост.» (воронка repeat_clients).
2. React key warning в DealsWorkspace (child from DealsPage) — вычистить.
3. Финальная сверка с эталоном `sales-board-mockup.html`: light + dark + 390px, расхождения — списком оператору, молча не срезать.

Ограничения: миграции НЕ заводить (голова 0089, номера — через координатора); скорборд не перестраивать; коммиты локально в `sales-2.0-redesign` БЕЗ push; бэк на `127.0.0.1:8000` (IPv4). Скрины — Playwright MCP.

DoD в ТЗ. По готовности: `КООРД: DONE sales — …` с коммитом.
