Добавь GET-эндпоинт `/healthz`, возвращающий `{"status": "ok"}`, в ядро платформы.

Это маленький пример-воркер, чтобы продемонстрировать поток оркестрации. Реальные
first-msg оркестратор генерирует под каждую подзадачу.

Цель (Goal-Driven): написать тест `tests/test_healthz.py`, который дёргает `/healthz`
через httpx ASGI и проверяет 200 + тело `{"status":"ok"}`, затем сделать его зелёным
минимальным изменением. Не трогать модули — только core/runtime.

Подробности и границы — в coordination/example-healthcheck-scope.md.
