# Чек-лист «включаем 1С» — что сделать в офисе

> Одна страница для похода в офис. Цель: чтобы **подбор товара**, **цена/себестоимость** и
> **остатки** в CRM ожили из живой 1С вместо демо/seed. Код-шов уже стоит (фасады ядра) —
> при живой 1С меняется только источник за фасадом, экраны не трогаем.
>
> Приоритет №1 — деньги: ошибка в цене/себесе = потери. Поэтому шаги 4–5 (тип цены и сверка)
> делаем на реальных данных вдумчиво. 1С — **строго чтение** (никакой записи, мастер-данные заморожены).

## Кто что делает

| # | Кто | Шаг | Готово? |
|---|-----|-----|:--:|
| 1 | Админ 1С | Поднять 1С-сервер (не работал вне офиса), проверить доступность по сети | ☐ |
| 2 | Админ 1С | Включить **OData** в 1С КА 2.4 (`192.168.89.14`) на нужные объекты — инструкция готова: `_docs_out/Инструкция_1С_OData_для_админа.docx` | ☐ |
| 3 | Оператор/DevOps | Дать бэкенду адрес 1С и креды: `AIOS_ONEC_BASE_URL` + логин/пароль сервис-пользователя 1С (только чтение) | ☐ |
| 4 | Оператор | Выбрать **тип цены** для продажи (оптовая/розничная/…) на реальной базе — от него зависит цена в КП/марже | ☐ |
| 5 | Оператор + я | Сверить **себес 1С vs себес закупок (landed)** на реальных числах — решить, что показываем и как метим | ☐ |
| 6 | Сессия `integrations` | Реализовать pull из 1С за фасадами ядра (детали ниже) | ☐ |
| 7 | Оператор/DevOps | Выключить демо-фикстуру: `AIOS_DEMO_PRICE_COST=0` (или убрать переменную) | ☐ |
| 8 | Оператор + я | Приёмка: подбор товара показывает реальную номенклатуру, карточка — метку «из 1С» | ☐ |

## Что именно тянуть из 1С (для сессии `integrations`, шаг 6)

Модули в 1С напрямую **не ходят** — только через фасады ядра. Наполняет их сессия
`integrations`; продажи только потребляют.

1. **Номенклатура (товары для подбора)** → справочник/MDM. Даёт код + название + единицу; по
   этому же эндпоинту `/sales/skus`, что сейчас отдаёт 23 демо-SKU, поедут реальные товары —
   **правок фронта не нужно**. Источник в 1С: справочник «Номенклатура».
2. **Цена продажи** → фасад `core.services.price_cost` (`PriceCostGateway.get_item_price_cost`).
   Источник в 1С: регистр «Цены номенклатуры» по **выбранному типу цены** (шаг 4).
3. **Себестоимость** → тот же фасад `price_cost`. Источник в 1С: стоимость товара/партии
   (учётная средневзвешенная). Это **не** China-landed из закупок — разные числа, отсюда сверка (шаг 5).
4. **Остатки** → **отдельный** фасад `core.services.stock` (`StockGateway`), не в справочник
   (транзакционные, 1С = истина). Источник в 1С: регистр остатков по складам.

Контракт фасада цены/себеса (уже в ядре, `core/services/price_cost.py`), заполнять эти поля на SKU:
`{cost_byn, price_byn, currency, price_type, as_of, source='onec'}`. Нет данных по коду → не кладём
(потребитель честно деградирует, показывает «не рассчитано», а не ноль).

## Что уже готово в коде (не переделывать)

- Фасад ядра `PriceCostGateway` + потребление в марже/КП/дефолтах плана + метки провенанса
  «из 1С»/«демо»/«из закупок» (коммиты PC1–PC6). Инвариант: без источника — прежнее честное
  поведение (нет фейковых денег).
- Список позиций карточки и метрики сделки берут маржу из **одного** фасада (свод сделан).
- Демо-фикстура `DemoPriceCostSource` — только dev+флаг, в проде мертва.

## Стоп-условие
Если на реальной 1С форма данных отличается от ожидаемой (другой регистр, поля) — **не гадаем**,
фиксируем и решаем на месте. Семантика сверки «1С-себес vs landed» (шаг 5) — решение оператора,
в коде помечено `BLOCKED-office` (`deal_margin_reconcile`).
