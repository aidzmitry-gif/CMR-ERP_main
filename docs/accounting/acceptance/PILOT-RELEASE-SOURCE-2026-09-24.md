# CRM-ACC-001 — состав исходников пилотного выпуска

Снимок: 24.09.2026, 07:00 UTC. Это инвентаризация исходников, **не собранный
и не выпущенный пакет**. Локальный root commit:
`ef3adb322a86d2e989b6725638b71e6a73c22f59` на
`agent/crm-acc-prod009`; его объект в `aidzmitry-gif/CMR-ERP_main` через
текущий авторизованный GitHub API вернул 404. Сам репозиторий доступен.
Рабочее дерево source содержит отдельный незатреканный
`reports/CRM-ACC-001/`, который не входит в commit и пакет.

Проверенный серверный baseline 24.09, 06:30 UTC: `/opt/cmr-erp` HEAD
`9a0238827a175db8e2952edbb64ae1d509b524f1` (checkout грязный),
работающий `aios-app-1` image ID
`sha256:4d86679ee28ea17494e3eeed7e183a0e838b4b19f5f1caf0e804d8c5ed441d9b`,
Alembic `0116`. Git HEAD грязного checkout не доказывает содержимое образа.
`9a023882` является предком локального `ef3adb32`; между ними 1625
изменённых путей: 572 frontend, 124 `modules/accounting`, 68 migrations,
387 tests, 32 `docs/accounting`, 38 других modules/gitlinks и 404 прочих.
Весь diff нельзя автоматически считать минимальным бухгалтерским релизом.
Локальный Alembic head — `0176`; синтетический переход и восстановление
проверены в `pg0116-to-0176-20260924.json`, без реальных данных.

| Gitlink | Точный SHA в root commit | Локальный commit object | GitHub API |
| --- | --- | --- | --- |
| finance | `59832b2f14a2ee02d8db266a67f8206616bdcd94` | есть | 404 |
| hr | `f8a6e0142f1920b07ec56818f42d156a602ff9c3` | есть | 404 |
| leads | `9a304f771ba74369ded199168e0e1da3ad6348d2` | есть | 404 |
| logistics | `6923d2e021980ef2546f382364d38dc34a0fef29` | есть | 200 |
| marketing | `d4ed67fdc9b41e7abc370112e1a3fa8a3fe1e282` | есть | 200 |
| procurement | `c2483d60b4bc22526cd8e2694053fd9eddfdc273` | есть | 200 |
| production | `be7d7e198bf09cc41c642d22125db401da5adc48` | есть | 404 |
| sales | `326b02034df21d137c2c9edf1bee3f9ae0682901` | есть | 404 |
| service | `3d33800c44865cd7a7373b29192c5d59d3c4bec8` | есть | 200 |
| wms | `c84ec34592fc984f9421ae264d406d9a258c6df9` | есть | 404 |

Gitlink SHA получены из `git ls-tree HEAD:modules`, а не из текущих
директорий. Девять каталогов `modules/*` в source worktree не содержат
собственного `.git`; их содержимое само по себе не подтверждает точный
gitlink. Локальные commit objects подтверждены в соседней восстановленной
копии модулей `crm-erpnext-final-20260914`; WMS подтверждён в собственном
репозитории `modules/wms` source worktree. GitHub API вернул 200 с точным SHA
для четырёх записей и 404 для шести; все шесть соответствующих репозиториев
доступны той же учётной записи. 404 означает, что точный объект сейчас
недоступен через этот API; это не доказательство отсутствия альтернативного
закрытого канала доставки. После публикации проверку нужно повторить.

Минимальные препятствия перед подготовкой выпуска:

1. Сделать root commit и все десять gitlink доступными выбранному
   воспроизводимому механизму доставки либо сформировать отдельный
   проверенный immutable пакет из точных локальных объектов. Нельзя брать
   файлы из грязного серверного checkout или подменять SHA ближайшей веткой.
2. Заморозить конкретный состав backend, frontend, миграций и submodules из
   1625 изменённых путей; проверить совместимость этого состава и собрать
   frontend/image заново. Сборка текущего состава этим манифестом не заявлена.
3. Для перехода с серверной `0116` восстановить актуальную резервную копию
   рабочей БД в изолированной среде и испытать `0176` на её данных. Локальная
   синтетическая репетиция не заменяет эту проверку.
4. Подготовить отдельный маршрут миграционного + frontend выпуска по
   `ops/belakb-deploy/START-HERE.md`; runner v1 поддерживает только
   backend без схемы/frontend. Затем нужны preflight, итоговый receipt и
   проверка пользовательского сценария. Здесь сервер не изменялся.

Реальные остатки и политика, независимая сверка с 1С, принятие бухгалтером,
два закрытия месяцев и годовой цикл остаются отдельными условиями отказа от 1С.
