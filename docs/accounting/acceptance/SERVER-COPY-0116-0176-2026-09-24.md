# G07P: репетиция `0116 → 0176` на копии текущей БД

Время: 24.09.2026, начало 07:33 UTC. На сервере belakb.by рабочие `aios-app-1` и `aios-postgres-1` не перезапускались и не мигрировали. Рабочая Alembic-ревизия до и после проверки — `0116`.

В защищённом каталоге `/opt/cmr-erp-backups/crm-acc001-20260924T073336Z-rehearsal` создан live-read custom `pg_dump` рабочей БД: 1841158 байт, SHA-256 `0d3bd395a2c58bf24e314d0412863aa813f98eb4a9f4fe361d716cd9663e3971`. Каталог имеет mode 700, dump и `archive.list` — mode 600; `pg_restore --list` прочёл архив (961 строк). Архив остаётся на сервере, на локальный компьютер не копировался.

Архив восстановлен в отдельный PostgreSQL-контейнер `crm-acc001-copy-20260924T073336Z` из image `sha256:00ba258a66dac104fd5171074a0084462a64a1369d8513f3d0a634e2f24d15bc`. Контейнер имел `--network none`, 0 открытых портов и отдельный анонимный volume; production volume не подключался. Восстановленная БД `aios_copy` имела ревизию `0116`. Кандидат `crm-acc-pilot-local:e5c81f92`, image ID `sha256:568d1040d04e9e73bae8043f73c345170893821eabc35de588564a23cf9a78dc`, передан через Tailscale SSH как Docker image и выполнил `alembic upgrade head` **только** против `aios_copy`. Итоговая ревизия копии — `0176`.

До миграции сохранены точные числа строк 114 исходных таблиц. После миграции имеется 258 таблиц; все 114 исходных таблиц найдены, ни у одной число строк не изменилось. Для проверки содержимого тот же архив отдельно восстановлен в `aios_before`: read-only скрипт [accounting_compare_copy_rows.py](../../../scripts/accounting_compare_copy_rows.py) сравнил исходные колонки каждой таблицы по отсортированным хешам строк. Служебная `alembic_version` исключена из сравнения содержимого: **113/113 таблиц и 49959 строк совпали**, нет утраченных исходных колонок. Серверный `content-compare.json` имеет SHA-256 `83098c8f4c674737a8166d1a953d4cc8e4be1d27a8a8a6082fe4ee96a3ec2d6c`.

После проверки временный контейнер и его единственный анонимный volume удалены; dump и компактные квитанции сохранены в защищённом каталоге. Загруженный на сервер образ кандидата не запускал приложение и не переключал действующий стек. Скрипт сравнения прошёл Ruff и `compileall`, его SHA-256 на локальном и серверном экземплярах совпал: `46b6377b9dd454625debbd21a665cf1cb614c9315e8785cbb2b0d501c6e81cda`.

Это репетиция на **живой прочитанной копии**, без остановки писателей. Она не заменяет согласованную резервную копию после остановки записи для отката выпуска, парное восстановление приватных файлов, production OIDC/frontend, пользовательский сценарий, сверку с 1С или разрешение на переключение. Реальный переход и отказ от 1С этим результатом не подтверждены.
