# G07P: чистый локальный исходник и backend image, 24.09.2026

Проверенный исходник: root commit `e5c81f92d087754d371e334a1ff138fa279894e8`, tree `ebe25a6c939f82f1c8507a1d6ef4bbd76a7eab86`. Скрипт `scripts/accounting_materialize_exact_source.py` взял только Git blobs этого коммита и десяти закреплённых gitlink-коммитов из локальных репозиториев. Все **2608** файлов проверены по Git blob SHA-1; root содержит 2358 файлов, модули — 250. Для каждого модуля исходный commit совпал с [манифестом пилота](PILOT-RELEASE-SOURCE-2026-09-24.md). Отсутствие `.env`, `.git` и незатреканного `reports/CRM-ACC-001/` в чистом каталоге проверено до сборки.

Первый пробный `git archive` отклонён: на Windows он выдал CRLF-байты как минимум для `.claude/settings.json.example`, отличающиеся от Git blob. Скрипт переключён на прямое чтение каждого blob через `git cat-file --batch` с проверкой его хеша. `ruff check`, `compileall` и точная материализация прошли.

Из полученного временного каталога `docker build --pull=false --progress=plain -t crm-acc-pilot-local:e5c81f92 .` завершился успешно на локальном Docker Engine 29.6.1, `linux/amd64`. Локальный image ID: `sha256:568d1040d04e9e73bae8043f73c345170893821eabc35de588564a23cf9a78dc`, размер по `docker image inspect` — 121234136 байт. Контейнер не запускался; этот образ не отправлялся на сервер или в registry.

Эта сборка устраняет неопределённость **локальной** комплектности исходника, но шесть gitlink-коммитов и root commit по-прежнему недоступны через проверенный GitHub API. Перед серверным пилотом отдельно нужны выбранный путь доставки точных исходников/образа, production frontend с действительными OIDC-настройками, восстановление копии текущей БД и миграция её `0116 → 0176`, маршрут смешанного выпуска по `ops/belakb-deploy/START-HERE.md`, а затем пользовательский сценарий и сверка с 1С. Ничего из этого локальная сборка не подтверждает.
