# Банковский участок: проверенное состояние

Проверено 13 сентября 2026 года на ветке `agent/crm-acc-contracts-20260913`, код до `7315205`. Это состояние локального пилота, а не разрешение отключить 1С.

## Что можно выполнить в пилоте

| Действие | Подтверждение |
| --- | --- |
| Явно закрепить импортированное поступление за юрлицом | API привязки; новая привязка не допускается для затронутого закрытого периода (`ec66600`) |
| Выбрать счета и заполнить обязательную аналитику | `255434e`; UI передаёт контрагента и договор в предварительный расчёт и подтверждение |
| Просмотреть счета, суммы и аналитику до проведения | `0e678ca`; данные берутся из серверного расчёта |
| Провести строку и повторить подтверждение без дубля | PostgreSQL-сценарий `test_imported_bank_source_is_mapped_atomic_and_immutable` |
| Открыть проводку из выписки | `e03c8b9`; браузерный сценарий проверяет номер операции и договор в существующей карточке |
| Найти непроведённые строки перед закрытием | `91cb338`; собственные строки до конца месяца и строки без даты блокируют закрытие через API |
| Безопасно выполнить конкурентные привязку и закрытие | `9556997`; обе очередности, два соединения, ожидание подтверждено `pg_blocking_pids` |

Браузерная проверка использует реальные Next/FastAPI и синтетическую SQLite-базу: два теста включая вход, 42,7 секунды. Данные учётной политики в этой проверке искусственные; они не подтверждают применимость нормативных правил.

## SQL-защита: проверена отдельно, установка ещё не завершена

`81ca584` содержит `modules/accounting/bank_period_guards.sql`. Четыре PostgreSQL-сценария с установленными триггерами прошли за 16,67 секунды. Проверены обычный импорт, повтор, обе очередности привязки/закрытия и запреты прямого SQL. Сумму и исходные реквизиты привязанной строки менять нельзя; метаданные сопоставления платежа обновляются.

Пакет пока не включён в зарегистрированную миграцию этой ветки. Исполнитель совместной версии CRM-ERPNext-001 получил его для отдельной миграции после объединяющей `0133`. На момент проверки общий реестр содержит `0133`, следующая ревизия не зарегистрирована. До принятия миграции SQL-защиту нельзя считать автоматически установленной приложением.

`7315205` добавляет читающую диагностику `modules/accounting/bank_period_preflight.sql`. Она выявляет старые привязки без источника/с неподходящей принадлежностью и закрытые периоды с непроведёнными строками. Проверка воспроизведённого старого расхождения на PostgreSQL прошла за 5,46 секунды; данные не исправляются автоматически.

## Что остаётся

- Включить SQL-пакет в зарегистрированную миграцию совместной версии; проверить обновление существующей базы и диагностику старых расхождений.
- Проверить резервное копирование и восстановление уже новой версии схемы. Ранее выполненная проверка `0132` не покрывает новые триггеры.
- Этот мост импорта предназначен для входящих поступлений в BYN. Валютные поступления и списания по выписке здесь не реализованы; отдельная форма банковской операции не является доказательством их автоматического импорта.
- Сопоставление старых строк без юрлица требует явного решения; они не относятся автоматически к основной компании. Очередь таких строк доступна главному бухгалтеру.
- Проверить работу на фактической выписке, утверждённой политике и согласованных остатках; сверить деньги и расчёты с 1С.
- Выполнить два последовательных месячных закрытия и остальные условия общего плана перехода. Локальные тесты и опубликованные коммиты не заменяют эти условия.

Общий backend CI для `069bf3f` при последнем наблюдении ещё выполнялся. Его результат нельзя переносить на последующие коммиты или на совместную версию с `0133`.
